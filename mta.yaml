ID: openSAPHana7HelloWorld
_schema-version: '2.1'
version: 0.0.1
modules:
  - name: openSAPHana7HelloWorld-db
    type: hdb
    path: db
    parameters:
      memory: 256M
      disk-quota: 256M
    requires:
      - name: openSAPHana7HelloWorld-hdi-container
  - name: openSAPHana7HelloWorld-srv
    type: nodejs
    path: srv
    parameters:
      memory: 512M
      disk-quota: 256M
    provides:
      - name: srv_api
        properties:
          url: '${default-url}'
    requires:
      - name: openSAPHana7HelloWorld-hdi-container
resources:
  - name: openSAPHana7HelloWorld-hdi-container
    properties:
      hdi-container-name: '${service-name}'
    type: com.sap.xs.hdi-container
